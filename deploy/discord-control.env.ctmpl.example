# The Control API signing key is the only Console-facing Discord secret.
# It is rendered from Vault and never sent to the browser.

{{- with secret "secret/data/muxivo-discord/staging" }}
MUXIVO_DISCORD_CONTROL_SIGNING_KEY={{ printf "%q" .Data.data.control_signing_key }}
{{- end }}
